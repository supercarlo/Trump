#parse("/velocity/layout.vm")
#@mainLayout()
<p>Order history</p>
    #set( $orderids = $history.GetOrderIDs("'admin'"))
    $orderids
    <br>
    $history.ShowOrderHistory($orderids)
    #set( $history = $history.ShowOrderHistory($orderids))

<style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }
</style>


#end
